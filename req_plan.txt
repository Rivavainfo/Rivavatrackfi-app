Okay, the plan is to:
1. Update `HomeScreen.kt` by using `sed` or directly creating a search and replace using `replace_with_git_merge_diff`.
   - The redesign will use a large immersive card with glassmorphism, gold outlines, etc., and will invoke `context.startActivity(Intent(context, EliteLandingActivity::class.java))` on click.
2. Create `EliteLandingActivity.kt` full-screen implementation at `app/src/main/java/com/rivavafi/universal/ui/elite/EliteLandingActivity.kt`.
   - It will use `EliteViewModel` to access `eliteConfig` and `eliteSubscription`.
   - Will implement the full design, scroll, and the WhatsApp deep link exactly as requested.
3. Update `AndroidManifest.xml` using `replace_with_git_merge_diff` to add the `EliteLandingActivity`.
4. Run tests/build to verify.
5. Pre-commit check and then submit.
